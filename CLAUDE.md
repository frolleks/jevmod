This project is a Discord bot written in TypeScript using Bun and discord.js.

For every new command, add the usage documentation to the help command, and the README.

Before finalizing your changes, run `bun fmt` to format your code.
